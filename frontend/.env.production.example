# Copy các dòng này vào Environment Variables của project trên Vercel (không copy
# thành file .env trong repo -- Vercel set qua dashboard, không đọc file .env.production
# từ git). Đổi giá trị thành URL backend Render thật sau khi deploy xong backend.

VITE_API_BASE_URL=https://edumind-ai-backend.onrender.com
